<?xml version="1.0" standalone="no" ?>

<svg version="1.1"
     xmlns:xlink="http://www.w3.org/1999/xlink"
     xmlns="http://www.w3.org/2000/svg"
     xmlns:svg="http://www.w3.org/2000/svg"
     width="{{width}}"
     height="{{height}}"
     viewBox="{{viewbox}}">

  <desc>
# TOML description

openqa-scenario = "{{ scenario }}"
openqa-test = "{{ test }}"
openqa-test-frame = "{{ test_frame }}"
openqa-last-update-id = "{{ test_id }}"
  </desc>
  <style type="text/css">
    <![CDATA[
    {{ css }}
    ]]>
  </style>

  <image width="{{original_width}}" height="{{original_height}}" xlink:href="{{ png }}" />

  <g>
    {%- for shape in shapes -%}
      {%- if shape['shape'] == 'rectangle' %}
	    <rect x="{{shape['x']}}" y="{{shape['y']}}" width="{{shape['width']}}" height="{{shape['height']}}" class="{{shape['class']}}"/>
      {%- else -%}
	    <text x="{{shape['x']}}" y="{{shape['y']}}" class="{{shape['class']}}">{{shape['text']}}</text>
      {% endif -%}
{%- endfor %}
  </g>
</svg>
